name: 🧮 Auto Update Stats

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: No-op Task (just to keep workflow alive)
        run: echo "Daily workflow executed successfully."

      - name: Commit Changes if Any
        run: |
          git config --global user.name "SandyieBot"
          git config --global user.email "dksanjay39@gmail.com"
          git add -A
          git commit -m "🔄 Daily maintenance run" || echo "No changes to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
